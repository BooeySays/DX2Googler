#!/bin/bash




echo -en """\033c
DX2Googler has the function to enable the
hotkey/keyboard shortcut feature used in
most GUI web browsers (except Microsoft's
Edge - It uses CTRL + e). By default, it
is enabled and set to [CTRL]+[k] (Just 
like the GUI web browser\'s counter part.
